import 'package:flutter/widgets.dart';

class TimingPlaceholder extends StatelessWidget {
  const TimingPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
