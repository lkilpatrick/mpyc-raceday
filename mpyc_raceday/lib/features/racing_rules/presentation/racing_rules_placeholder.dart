import 'package:flutter/widgets.dart';

class RacingRulesPlaceholder extends StatelessWidget {
  const RacingRulesPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
