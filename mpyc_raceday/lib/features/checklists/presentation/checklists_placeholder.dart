import 'package:flutter/widgets.dart';

class ChecklistsPlaceholder extends StatelessWidget {
  const ChecklistsPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
