import 'package:flutter/widgets.dart';

class IncidentsPlaceholder extends StatelessWidget {
  const IncidentsPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
