import 'package:flutter/widgets.dart';

class BoatCheckinPlaceholder extends StatelessWidget {
  const BoatCheckinPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
