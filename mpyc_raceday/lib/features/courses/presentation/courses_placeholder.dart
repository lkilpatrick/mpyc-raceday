import 'package:flutter/widgets.dart';

class CoursesPlaceholder extends StatelessWidget {
  const CoursesPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
